/**
 * Гайд писателя — RU-пресет. Host-wide фрагмент слоя 5; см. заголовок
 * guide-en.ts. Семантическое зеркало EN-базы с RU-таксономией; стилевая
 * база — живой гайд референс-реализации.
 */

export const GUIDE_RU = `# iapeer-memory — общая память команды

Общая память команды (агенты + человек): канон + личная оперативка каждого агента. Ты читаешь и пишешь.

**Чтение.** Существенную задачу (ресёрч, решение, повторяющаяся проблема, продолжение чужой работы) начинай с поиска в памяти — команда могла уже решить. Заметка — снимок на момент записи: проверяй перед действием; при конфликте с живым наблюдением доверяй наблюдению и обнови или депрекируй заметку.

**Запись.** Между сессиями тебя нет — не записанное в vault теряется; что и куда писать, решаешь сам, не спрашивая человека. Пиши кратко: заметки попадают в контекст всей команды, раздутость стоит токенов каждому читателю. Записал новое → сразу поставь финальный \`status\` прежней заметке по теме, если она устарела.

## Хендофф — только по триггерам

Каждая правка хендоффа жжёт токены (твои на запись, команды на чтение), поэтому пиши его не по ощущению «веха», а только на событиях:

- перед self-fresh, рестартом или рискованной операцией;
- контекст заполнен на ~80%+;
- закрываешь ход с открытым хвостом, продолжение которого ничем не гарантировано (нет watcher'а, таймера, ожидаемого входящего).

Дедуп: в хендофф идёт только то, что не восстановимо из другой durable-поверхности (План/Фаза, реестры, IAP-история) — дублировать их бессмысленно. Закрытие нити — не повод для правки: вычищай закрытое при следующем триггерном снимке. Держи ОДИН скользящий хендофф на активную нить: стабильный title без даты (дата в теле), тело переписывай на месте. Нить завершилась → \`status: устарело\`. Эволюционирующий материал консолидируй на тех же триггерах, не по микрошагам.

## Структура хранилища

| Папка | Что лежит | \`type\` | \`status\` |
|---|---|---|---|
| \`01_Знания/\` | Факты, описания систем, справочные таблицы, обобщённые до паттерна инциденты | \`знание\` | \`актуально\` → \`устарело\` |
| \`02_Решения/\` | Выборы из альтернатив с обоснованием; иммутабельны по факту решения | \`решение\` | \`принято\` → \`заменено\` |
| \`03_Проекты/<имя>/\` | \`Описание <имя>.md\`, \`План <имя>.md\`, \`Фаза — <название>.md\` | \`проект\` | Описание/План: \`активный\` / \`на паузе\` / \`завершён\`. Фаза: \`запланирована\` / \`активная\` / \`завершена\` / \`на паузе\` / \`отменена\` |
| \`04_Идеи/\` | Гипотезы, «может пригодиться» | \`идея\` | \`новая\` → \`реализуется\` → \`отброшена\` |
| \`05_Списки/\` | Реестры, словари, трекеры (append-only), профили внешних сущностей | \`список\` | \`актуально\` → \`устарело\` |
| \`06_Оперативка_агентов/<имя>/\` | Личная оперативка агента | \`оперативка агентов\` | \`актуально\` → \`устарело\` |

## Как писать

**Файловым тулом рантайма, не bash** (claude — \`Write\`/\`Edit\`, codex — \`apply_patch\`): запись через bash минует пост-райт хук — memoryd припишет правку человеку и не проставит автора.

**Канон** (папки 01–05) — стабильный материал, не привязанный к одной задаче. Ракурс — объективное знание о системе, безлично («система делает X»); субъективное и процедурное «как действую я» — в оперативку. Стиль: идиоматичный русский, академический тон, самодостаточный текст (без отсылок к разговору, аббревиатуры расшифровывай), без эмодзи; гипотезы помечай («Похоже, X»). Материал и для команды, и лично для тебя → пиши оба: канон + оперативочная заметка со ссылкой \`[[...]]\` на него.

От тебя в канон-заметке: самоописательный TITLE (= имя файла — читатель индекса должен понять содержание, не открывая), тело со связями, ≥1 тег. Frontmatter: \`tags: [Тег1, Тег2]\`. Путь: \`{{VAULT_PATH}}/<папка типа>/<Название>.md\`. Теги — из словаря \`99_Система/Теги.md\` там же; нет подходящего — добавь в словарь явно (не на лету в заметке) и ставь: добавляет любой автор, Index курирует дедуп.

**Связи** — два равноправных вида, оба уходят в граф: inline \`[[X]]\` в тексте, когда заметка — часть фразы; блок \`## Связи\` в конце заметки для смежных, которых в тексте нет — каждая строка \`- [[X]] — чем связана\`, пояснение обязательно (оно же защищает от линковки вслепую).

**Оперативка** — личное, в \`06_Оперативка_агентов/<твоё имя>/\`. Frontmatter: \`subtype\` + \`description\` (1–2 предложения). Subtype: \`обратная_связь\` (от коллег) · \`контекст\` (рабочий контекст, handoff) · \`справка\` (навигационные метки, процедурный приём) · \`профиль_человека\` · \`грабли\` (правило после конкретного инцидента).

## Правки и удаление

Канон — командное знание: правь свободно, свой и чужой, прямо в тексте, без журналов правок. Оперативку правит только автор. Из frontmatter меняешь ТОЛЬКО \`status\` (шкала — в таблице выше); \`needs_review\` ставит и снимает механика/Индекс — после твоей правки оставь его как есть и продолжай работу, уведомлять никого не нужно (Индекс обходит флаги фоном).

Заметки не удаляй: финальный \`status\` и есть удаление — memoryd переносит в \`07_Архив\` (связи целы, заметка ищется со stale-штрафом).

**Проекты:** \`План <имя>.md\` — список фаз, \`Фаза — <название>.md\` — чеклист задач фазы; оба append-only — дописывай, не переписывай историю.
`;
