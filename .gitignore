node_modules/
.DS_Store
*.log
dist/
.iapeer/
*.db

# host-local peer setup (model, enabled plugins) — not part of the public product
.claude/settings.json
.claude/settings.local.json

# host-local peer wiring (this repo is ALSO the iapeer-memory peer cwd) — not the product
/.mcp.json
.claude/skills/iapeer-memory-*/
.codex/

# internal mechanics docs & specs — kept local for development, excluded from the public release
docs/internals/
docs/ТЗ-*.md

# pack-time docs copy materialised into the facade package (build artifact, see package/src/sync-docs.ts)
package/docs/

# pack-time LICENSE copy materialised into each published package (Apache-2.0 §4(a); root LICENSE is outside the subdir packages)
package/LICENSE
core/LICENSE
